# 模型编排与治理辅助政策

状态：提议进入生产治理

## 结论

网页 GPT 是整个决策系统唯一的语义总控、唯一对外入口和三个业务中心之间的唯一中继。

`decision-system-governance` 继续作为确定性政策、接口合同、版本矩阵、验收门和恢复规则的权威来源。治理仓库本身：

- 不运行业务任务；
- 不调用外部 API；
- 不调用模型；
- 不保存业务 Secret；
- 不保存业务 Artifact；
- 不成为第四个业务中心；
- 不直接连接情报中心、计算中心或专家研判中心。

因此，**不在治理仓库或三个业务中心内新增管理模型**。

网页 GPT 在处理大型跨仓库审计时，可以在其自身会话内临时使用一次只读审阅模型，但该模型属于网页 GPT 的外部辅助过程，不部署到治理仓库，不持有仓库写权限，不形成新的中心或控制面。

## 角色边界

### 网页 GPT

- 唯一理解用户目标、拆解任务和决定调用顺序的语义控制面；
- 唯一分别与治理仓库及三个业务中心通信的上层入口；
- 负责将自然语言转换为固定票据；
- 负责核对 Issue、Actions、哈希、Artifact 和生产门；
- 负责最终向用户解释结果；
- 不得绕过确定性门禁声明生产通过。

### 治理仓库

只保存：

- 总体架构和三中心隔离合同；
- 票据与跨仓库接口 Schema；
- 版本矩阵和冻结基线；
- 安全边界、验收标准、恢复与回滚手册；
- 顶层宪法及其确定性检查规则。

治理仓库不在业务运行时跨仓库读取、下载或控制其他中心，也不通过 `repository_dispatch`、共享运行工作流或后台服务连接三个中心。

### 网页 GPT 外部只读审阅

仅在跨仓库架构变更、生产发布审计、复杂失败链分析或顶层宪法核对时，由网页 GPT 按需执行一次。允许输入：

- PR diff；
- Issue 票据；
- Actions 回执；
- Artifact manifest；
- 治理规则的只读副本。

允许输出：

- 规则命中；
- 风险和遗漏；
- 缺失证据；
- 建议补充的测试；
- 供网页 GPT 综合的审阅意见。

该外部辅助过程必须满足：

- 无常驻进程；
- 无跨任务记忆；
- 无自主循环；
- 无 GitHub 写权限；
- 无合并、发布、回滚或 Secret 权限；
- 不直接调用三个中心；
- 不替代测试、Schema、哈希、签名、人工授权或生产门；
- 失败时直接退化为网页 GPT 加确定性治理规则。

## 禁止的架构

- 在三个业务中心分别放置管理模型；
- 在治理仓库部署模型调用或模型管理服务；
- 建立自主派发任务或修改仓库的第四 AI 中心；
- 让第二个模型与网页 GPT 同时拥有最终决策权；
- 让模型根据历史偏好自行改变顶层宪法；
- 建立无限 Agent 循环、模型互评循环或后台长期运行服务；
- 以模型判断替代可复算的证据和确定性验收。

## 固定调用链

```text
用户
→ 网页 GPT（唯一语义总控与中继）
→ 读取治理仓库的冻结规则副本
→ 必要时在网页 GPT 内执行一次外部只读审阅
→ 网页 GPT 分别向单个业务中心提交固定票据
→ GitHub Actions 确定性执行与审计
→ 网页 GPT 汇总回执并向用户解释
```

三个业务中心之间始终没有直接连接。

## 最终权责

- 用户：最终授权者；
- 网页 GPT：唯一语义编排者、唯一中继和对外责任主体；
- GitHub Actions：唯一确定性执行与审计平台；
- 治理仓库：模型无关的治理规则权威；
- 外部只读审阅模型：可选、临时、无权、只服务于网页 GPT。

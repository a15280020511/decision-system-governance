# 模型编排与治理辅助政策

状态：提议进入生产治理

## 结论

网页 GPT 是整个决策系统唯一的语义总控与任务编排者。治理仓库继续作为确定性政策、接口合同、版本矩阵、验收门和恢复规则的权威来源，不新增常驻、自主、可写仓库的第二管理模型。

可以配置一个按需调用、无状态、只读的“治理辅助模型”，但它不是第四中心，也不是第二总控。它只负责审阅和提出建议，所有任务派发、跨中心材料转交、代码修改、合并、生产发布和用户沟通仍由网页 GPT 发起，并由确定性 GitHub Actions 门禁约束。

## 角色边界

### 网页 GPT

- 唯一理解用户目标、拆解任务和决定调用顺序的语义控制面。
- 唯一允许分别与情报中心、计算中心、专家研判中心和治理仓库通信的上层入口。
- 负责把自然语言转换为固定票据，并核对回执、证据和 Artifact。
- 不允许绕过仓库门禁直接声明生产通过。

### 治理仓库

- 保存架构合同、中心边界、票据 Schema、生产基线、版本矩阵、验收条件、恢复手册和安全政策。
- 只提供确定性规则和只读治理上下文，不承担业务分析、数据采集、数值计算或专家研判。
- 不直接连接三个业务中心；中心之间继续禁止直连。

### 可选治理辅助模型

仅在网页 GPT 明确调用时执行一次只读审阅，可用于：

- 对 PR diff、Issue 票据、Actions 回执和 Artifact manifest 做一致性检查；
- 检查变更是否违反中心隔离、禁止工具、断网、单任务、证据回执等顶层规则；
- 将复杂仓库状态压缩成供网页 GPT 使用的结构化建议；
- 对高风险变更提供反对意见、遗漏清单和验证建议。

它必须满足以下限制：

- 无常驻进程、无跨任务记忆、无自主循环；
- 默认只读，禁止直接创建提交、合并 PR、移动生产分支、修改 Secret 或触发付费调用；
- 不持有中心 API Key、GitHub 写令牌或用户隐私数据；
- 不直接调用情报、计算或专家中心；
- 不替代确定性测试、Schema 校验、哈希校验、人工批准和生产门；
- 输出必须明确区分事实、规则命中、推断和建议；
- 单次执行失败时，系统退化为网页 GPT 加确定性治理规则，不影响中心运行。

## 禁止的架构

- 在三个业务中心分别放置管理模型；
- 建立一个可自主派发任务、修改仓库或互相调用中心的“第四 AI 中心”；
- 让治理模型和网页 GPT 同时拥有最终决策权；
- 让模型根据历史偏好自行改变顶层宪法或生产门；
- 建立无限 Agent 循环、模型互相评审循环或后台长期运行服务；
- 以模型判断替代可复算的测试、证据和签名回执。

## 推荐调用链

```text
用户
→ 网页 GPT（唯一语义总控）
→ 治理仓库读取规则与版本
→ 可选治理辅助模型进行一次只读审阅
→ 网页 GPT 分别向单个中心提交固定票据
→ GitHub Actions 确定性执行与审计
→ 网页 GPT 汇总回执并向用户解释
```

三个业务中心之间仍然没有任何直接连接。

## 启用条件

治理辅助模型只有在下列任务中才有明显收益：

- 跨多个仓库的架构变更；
- 生产发布、回滚和版本漂移审计；
- 大型 PR 或复杂 Actions 失败链分析；
- 顶层宪法符合性审查；
- 网页 GPT 需要压缩大量仓库证据时。

普通数据查询、单次计算和专家研判不调用治理辅助模型，避免增加延迟、费用和新的不确定性。

## 最终权责

- 用户：最终授权者。
- 网页 GPT：唯一语义编排者和对外责任主体。
- GitHub Actions：唯一确定性执行和审计平台。
- 治理仓库：唯一治理规则来源。
- 治理辅助模型：可选、无权、只读、一次性顾问。

# 模型编排与治理辅助政策

状态：候选生产政策

## 结论

网页 GPT 是整个决策系统唯一的语义总控、唯一对外入口和三个业务中心之间的唯一中继。

`decision-system-governance` 是确定性政策、接口合同、版本矩阵、验收门和恢复规则的权威来源。治理仓允许部署一个**无权治理副驾驶选择器**，但选择器与实际模型推理严格分离。

当前允许的阶段仅为：

```text
OpenRouter 只读模型目录与基准读取
→ 确定性筛选付费通用旗舰候选
→ 输出可审计选择回执
```

当前禁止：

```text
调用模型推理端点
自动修改仓库
自动派发三个中心
自动合并、发布或回滚
自动改变顶层宪法
```

实际治理副驾驶模型推理必须经过独立付费 Canary、结构化输出验收和单独生产 PR；在该 PR 合并前保持关闭。

## 角色边界

### 网页 GPT

- 唯一理解用户目标、拆解任务和决定调用顺序的语义控制面；
- 唯一分别与治理仓库及三个业务中心通信的上层入口；
- 负责将自然语言转换为固定票据；
- 负责核对 Issue、Actions、哈希、Artifact 和生产门；
- 决定是否采纳治理副驾驶建议；
- 负责最终向用户解释结果；
- 不得绕过确定性门禁声明生产通过。

### 治理仓库

保存并执行：

- 总体架构和三中心隔离合同；
- 票据与跨仓库接口 Schema；
- 版本矩阵和冻结基线；
- 安全边界、验收标准、恢复与回滚手册；
- 顶层宪法及其确定性检查规则；
- OpenRouter 模型目录选择器及其测试和审计回执。

治理仓不成为第四个业务中心，不在业务运行时直接连接情报中心、计算中心或专家中心，也不通过 `repository_dispatch`、共享运行工作流或后台服务绕过既有控制面。

## OpenRouter 目录选择器

### 允许范围

选择器只允许使用：

```text
GET https://openrouter.ai/api/v1/models
GET https://openrouter.ai/api/v1/benchmarks
```

只允许读取：

- 模型标识和产品层级；
- 实时价格与生命周期；
- 输入输出模态；
- Intelligence、Coding、Agentic 基准；
- 官方目录排序和基准元数据。

选择规则：

1. 排除免费、过期、Preview、Beta、Experimental；
2. 排除 Flash、Mini、Nano、Micro、Small、Lite、Fast、Instant、Turbo、Haiku 等经济或速度层级；
3. 排除 Coder、Code、Safety、Guard、Embedding、Rerank、Moderation 等领域专用型号；
4. 只保留付费、稳定、通用文本模型且三项基准完整；
5. 旗舰身份来自正式产品层级，或该公司实时能力分布中的自然最高层；
6. 保持 OpenRouter `pricing-low-to-high` 官方顺序；
7. 选择剩余通用旗舰候选中的第一个模型。

### Secret 与权限

- 唯一允许的模型市场 Secret 名称为 `OPENROUTER_API_KEY`；
- Secret 只存在于治理仓 GitHub Actions Secrets；
- 禁止写入代码、日志、Artifact、Issue、PR 或模型输入；
- 目录工作流仓库权限固定为 `contents: read`；
- 禁止 `contents: write`、`issues: write`、`pull-requests: write`、`actions: write` 和 `id-token: write`；
- 禁止调用 `/chat/completions`、`/responses` 或其他推理端点；
- 目录筛选模型调用数和模型费用必须始终为 0。

### 失败策略

- 网络或上游临时失败只进行一次有限重试；
- 429、5xx、超时、损坏 JSON、空目录、字段错误或无合格旗舰时全部失败关闭；
- 禁止无限重试；
- 禁止静默降级为免费模型或普通模型；
- 禁止自动切换到上一次模型；
- 失败不得触发模型推理、仓库修改或中心派发；
- 上游目录变化时重新计算，不保留跨任务偏好或历史权重。

## 治理副驾驶模型

目录选择器输出的模型仅是**候选模型**。在独立付费 Canary 通过前，不具备生产推理资格。

未来启用后，治理副驾驶只允许：

- 代码和 Actions 日志诊断；
- 最小补丁草案；
- 红队审阅；
- 路由建议；
- 测试和回滚建议；
- 治理合同冲突检查。

始终禁止：

- 直接提交代码或修改分支；
- 合并 PR、发布、回滚或修改 Secret；
- 直接控制情报、计算或专家中心；
- 自主循环、模型互评循环或跨任务记忆；
- 替代 Schema、测试、哈希、Artifact、人工授权和生产门；
- 与网页 GPT 共享最终决策权。

## 长期健康检查

- 代码或工作流变化时执行双 Ubuntu 版本离线回归和随机压力测试；
- 执行上游 HTTP 故障、权限和零推理边界测试；
- 每周执行一次 OpenRouter 只读实时目录合同检查；
- 每次实时检查连续读取三次目录并验证选择结果、候选顺序和价格快照一致；
- 周期检查只生成 Actions 回执和短期 Artifact，不自动修改仓库或模型配置；
- 健康检查失败时保持上次生产配置不变，并将本次选择视为无效。

## 固定调用链

```text
用户
→ 网页 GPT（唯一语义总控与中继）
→ 治理仓确定性目录选择器（只读、零推理）
→ 生成候选模型与审计回执
→ 独立 Canary 与生产门（实际推理启用前）
→ 网页 GPT 决定是否采纳治理副驾驶建议
→ GitHub Actions 确定性执行与审计
→ 网页 GPT 汇总回执并向用户解释
```

三个业务中心之间始终没有直接连接。

## 最终权责

- 用户：最终授权者；
- 网页 GPT：唯一语义编排者、唯一中继和对外责任主体；
- GitHub Actions：唯一确定性执行与审计平台；
- 治理仓库：治理规则、目录选择器和验收门权威；
- OpenRouter 目录选择器：只读、确定性、零推理；
- 治理副驾驶模型：待独立 Canary 后启用的无权建议者。

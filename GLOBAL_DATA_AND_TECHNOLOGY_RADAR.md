# 全球数据与免费技术雷达

机器权威文件：`GLOBAL_DATA_TECH_RADAR_POLICY.json`。

## 目标

系统不再只搜索机构 API，而是持续覆盖两套全球对象：

1. **全球数据宇宙**：API、开放数据、数据库、实时流、传感器、网页、PDF、研报、论文、年鉴、法规、公司披露、地图、遥感、模型和数据集。
2. **全球技术宇宙**：适合情报中心、计算中心和治理中心的开源包、算法、模型、数据集、MCP、连接器、协议适配器、工程工具和安全工具。

任何有限搜索都不能诚实保证“全球一个不少”。本系统用注册表优先、持续增量发现、覆盖矩阵和缺口审计，追求可度量地接近完整，而不宣称虚假的绝对完整。

## 三中心职责

- **治理中心**：保存权威分类、覆盖矩阵、审批状态、版本和哈希、淘汰记录、证据回执。
- **情报中心**：外部发现、读取、规范化、来源验证、许可证与额度记录、网页和文档处理。只允许固定只读端点自动登记。
- **计算中心**：断网评测和运行已经批准、固定版本、可复现的免费工具。禁止运行时联网、动态下载依赖或执行未知代码。
- **专家中心**：继续禁止工具，只接收整理后的证据和计算结果。

## 双雷达结构

### 数据源雷达

覆盖：

- 所有国家、地区、语言和公开机构类型；
- 宏观、金融、商业、公司、贸易、能源、农业、医疗、科技、制造、地理、交通、法律、社会、安全、文化等全部业务域；
- REST、OpenAPI、GraphQL、SPARQL、SDMX、CKAN、Socrata、ArcGIS、STAC、OAI-PMH、SRU、IIIF、RSS、Sitemap、CSV、Parquet、RDF、NetCDF、HDF5、COG、WMS/WFS 等接口与格式；
- API之外的网页、PDF、Excel、研报、论文、年鉴、公告、档案、公司披露和数据快照。

### 技术雷达

覆盖：

- PyPI、conda-forge、CRAN/R-universe、Julia General、npm、Maven、NuGet、RubyGems、crates.io、Go modules；
- GitHub、GitLab、Bitbucket、Software Heritage、Zenodo；
- Hugging Face 模型、数据集、Spaces/MCP；
- OpenML 数据集、任务、运行和基准；
- 搜索、浏览、解析、ETL、校验、实体消歧、数据血缘、知识图谱、统计、计量、因果、贝叶斯、预测、优化、仿真、博弈、网络科学、遥感、金融分析、可视化、复现、安全和依赖审计工具。

## 搜索顺序

1. 官方注册表和目录；
2. 官方机构开发者门户、开放数据门户和文献仓库；
3. 软件包、模型、数据集和科研注册表；
4. 多语言网页搜索；
5. 代码仓库和 MCP 搜索；
6. 从已信任来源继续枚举链接、协议和关联机构。

通用搜索引擎负责补漏，不承担全球主目录职责。

## 查询模板

不要把所有条件塞入一条超长查询。按目标轮换：

- 数据源：`[国家/语言] [行业] [机构] (API OR open data OR database OR dataset)`
- 协议：`[机构/行业] (OpenAPI OR GraphQL OR SDMX OR SPARQL OR OAI-PMH OR STAC)`
- 文献：`[机构/行业] (report OR working paper OR yearbook OR publication repository)`
- 工具：`[能力] (library OR package OR framework OR toolkit OR SDK)`
- MCP：`[能力/数据源] (MCP server OR Model Context Protocol OR connector)`
- 技术变化：`[工具/协议] (release OR changelog OR deprecated OR migration OR benchmark)`
- 本地语言：每个国家使用当地语言的“开放数据、接口、数据库、数据集、研究报告、软件包、工具”等词。

## 免费门

只有以下类型可进入候选池：

- 开源；
- 公共领域或开放数据；
- 无 Key 免费；
- 免费注册后获得 Key；
- 免费快照或公开批量下载；
- 明确硬停止、不会自动产生费用的免费额度。

必须拒绝：试用期、自动转付费、必须绑卡且可能产生费用、许可证不明、禁止自动访问、绕过付费墙、收集私人数据。

## 工具晋级流程

```text
发现
→ 元数据规范化
→ 去重
→ 官方来源核验
→ 许可证与费用核验
→ OSV/依赖/供应链安全检查
→ 维护度与社区检查
→ 中心职责匹配
→ 隔离沙箱
→ 固定基准测试
→ Canary
→ 治理审批
→ 固定版本和哈希
→ 生产晋级
→ 持续监控
→ 降级或淘汰
```

工具不能因为“热门”自动加入。只有填补缺失能力，或在质量、安全、可复现、维护度、资源消耗、基准表现上明显优于现有工具，才允许晋级。

## 调度

### 每日

- 新发布、变更、停用、漏洞、关键数据更新；
- RSS、Atom、Sitemap、注册表增量；
- 到期来源健康检查；
- 不执行全球全量搜索。

### 每周

- 全球国家、语言、行业、机构、协议和工具能力的平衡轮换；
- APIs.guru、包注册表、模型/数据集注册表、GitHub/MCP 增量；
- 候选去重、评分和优先级调整。

### 每月

- 免费额度、绑卡、自动扣费、许可证、速率限制、维护状态复查；
- 已晋级计算工具重新跑基准；
- 失效源和被替代工具淘汰；
- 生成全球覆盖缺口图。

### 每季度

- 前沿技术与架构评审；
- 对非洲、中东、拉美、中亚、小语种和中国大陆专业源专项补漏；
- 识别能显著替代现有技术的免费新工具。

## 安全边界

自动流程可以登记元数据、读取固定公开端点、下载小型有界样本、创建评审票据和发送 Server酱通知。

自动流程禁止：

- 安装新发现的包；
- 执行未知代码或本地 MCP；
- 运行未知容器或二进制；
- 开启写 API；
- 自动提交密钥；
- 自动升级生产依赖；
- 自动开启付费或超额计费。

## 覆盖指标

必须按以下维度统计，而不是只统计“找到多少 URL”：

- 国家、地区、语言；
- 行业、机构类型；
- 数据源类型、协议和格式；
- 工具生态和能力分类；
- 免费模式与许可证；
- 更新频率；
- 新增高价值源、误报率、重复率、失效率；
- 安全淘汰率、额度消耗；
- 新工具相对现有基准的增益。

## 结论

保持领先不是一次性安装最多工具，而是建立持续发现、严格筛选、基准竞争、版本固定和自动淘汰机制。数据尽量广收，工具必须少而精；候选库可以无限增长，生产能力库必须受治理控制。

# 情报中心合规审计报告

审计日期：`2026-08-03`

审计对象：`a15280020511/evidence-data-center`

审计基线：`main@bbae64bda52eb9160b6fc3e62acc9e3010189ff4`

审计范围：仓库中可见的 Provider、票据 Schema、GitHub Actions 工作流、数据归档、网页读取、公司数据接口和拟议合规门禁。

审计限制：本次未取得中经、天眼查等实际合同文本，也无法从代码仓库确认 Cloudflare、Hugging Face、GitHub 和 GPT 账户后台的地域、保留期及组织级设置。因此对合同许可和平台账户设置作条件判断，不作无证据推定。

## 一、结论

```text
总体状态：部分合规
技术访问边界：较好
来源授权门：不完整
内容分类门：不完整
境外归档门：存在生产级缺口
合规门接入状态：未完成
正式生产合规资格：BLOCKED
```

当前系统已大量采用固定官方主机、只读操作、单次有界请求、禁止任意 URL 或任意函数、禁止写操作、禁止自动重试等正确设计。这些措施能够降低越权、失控调用和系统干扰风险。

但现有生产主分支还不能对每次任务证明：

1. 数据源明确允许自动化和境外访问；
2. 商业合同允许 GitHub、Cloudflare、Hugging Face 或 GPT 处理和存储；
3. 响应内容确实不含个人信息、重要数据或合同限制数据；
4. 原始结果不会通过公开仓库的 Artifact、日志或评论不当暴露；
5. 所有 Provider 都执行同一套合规门禁。

因此不能宣布完全合规或安全。

## 二、风险的本质

风险不在“外国 IP”本身，而在以下七个问题：

```text
有没有权取得
有没有绕过取得
取得的是什么数据
取得了多少
拿去做什么
保存到哪里、谁能访问
发生争议时能否证明全过程合规
```

外国 IP、GitHub、Cloudflare、Hugging Face 和 GPT 只改变执行和存储地点，不改变来源授权。固定境外出口可以用于合规任务；代理池、IP 轮换和多地区切换一旦用于绕过拒绝、限流或地域控制，即成为高风险规避行为。

## 三、已经符合或方向正确的部分

### 1. Provider 普遍采用有界只读设计

统一测试证明当前目录包含 68 个普通连接器和 580 项托管操作；多数 Provider 固定官方主机，禁止任意 URL、任意函数、写操作、交易和自动分页。

### 2. 浏览器工具主动关闭高风险能力

Browserless 已禁止代理配置、Cookie、自定义请求头、验证码或 unblock、任意代码和 WebSocket 会话。Firecrawl 禁止浏览器交互、actions、自定义请求头和异步全站爬取。Tavily 对页数和深度作限制。

### 3. 已对上游明确拒绝作出正确处理

国家数据网站对 GitHub 公有云出口返回 `403/WAF UrlACL` 后，相关直连操作已从生产目录删除，没有通过更换 IP 或其他节点继续绕过。

### 4. 天眼查使用正式开放接口而非网页抓取

公司情报 Provider 固定调用天眼查开放 API，并递归删除手机号、邮箱、身份证、联系人、法定代表人或人员姓名等敏感键，方向正确。

### 5. 密钥和调用边界总体较严

多数 Provider 使用后端 Secret，票据不能提交自定义凭据；工作流固定依赖版本，限制响应大小、超时、请求次数和并发。

## 四、主要不合规缺口

### P0-1：完整 Cloudflare 采集结果被归档到境外 Hugging Face

成功的 Cloudflare 采集会把完整结果文件上传到私有 Hugging Face Dataset，并采用追加式版本化长期归档。

归档程序只验证任务票据声明：

```text
classification = public
contains_personal_data = false
```

未发现上传前对实际响应内容进行独立的个人信息、重要数据、商业数据库内容或合同限制数据扫描。私有仓库只能限制访问，不能替代来源许可和数据出境判断。

整改：在归档前加入内容级 DLP、个人信息和高风险字段扫描；未通过时进入隔离区，不得上传境外。

### P0-2：公开 GitHub 仓库上传完整结果 Artifact

仓库是公开仓库。Cloudflare、网页读取和天眼查工作流都会把结果目录上传为 GitHub Actions Artifact，保留 30 天，并在公开 Issue 评论中发布 Artifact 链接。

网页读取 Artifact 可能包含完整网页正文；公司情报 Artifact 包含清洗后的 API 结果；Cloudflare Artifact 包含浏览器渲染、页面、截图、PDF、DOM 或扫描结果。

即使结果原本公开，响应中仍可能夹带个人信息、合同限制内容、内部页面信息或错误响应。当前仅依赖票据自报，不能保证 Artifact 内容符合公开发布条件。

整改：公开仓库只上传元数据、哈希和脱敏摘要；原始数据不得进入公开仓库 Artifact。确需保存时，应进入经过批准的私有证据库并设置最短保留期。

### P0-3：票据缺少法律授权和跨境字段

Cloudflare、Jina、Exa和天眼查票据主要要求任务 ID、操作、参数以及“公开、无个人信息”的自我声明，但没有强制记录：

- 数据所有者；
- 授权依据；
- 条款或合同快照哈希；
- 是否允许自动化；
- 是否允许外国 IP；
- 是否允许第三方处理；
- 实际存储位置；
- 保存期限；
- 是否允许再分发。

整改：所有联网工作流必须在请求前通过统一合规票据 Schema，缺少关键字段时失败关闭。

### P1-1：任意公网网页工具缺少来源准入表

Cloudflare Browser Rendering、Jina Reader、Exa Contents、Tavily、Firecrawl 和 Browserless 可以处理调用方提供的公网 URL。现有代码主要检查 HTTPS、公共 IP、请求数量和技术参数，没有看到统一的政府域名白名单、商业数据库阻断表、Robots/条款核验或来源授权登记。

整改：建立来源登记表，将来源分成官方开放、人工复核和禁止三类；中经、天眼查、企查查等商业数据库网页必须强制阻断，只允许正式接口。

### P1-2：目标网站的拒绝信号不一定能够被代理工具识别

直接 HTTP Provider 会把非 2xx 状态视为失败且通常不自动重试，这是正确的。

但通过 Browser Rendering、Jina、Exa、Tavily、Firecrawl 或 Browserless 访问时，第三方服务自身可能返回 HTTP 200，同时正文实际是验证码、WAF 或封禁页面。仅检查第三方 API 状态无法保证识别目标站点的拒绝。

整改：对返回正文、标题、页面结构和截图增加 CAPTCHA、WAF、Access Denied、登录验证和地域限制检测；命中后标记 `STOPPED_BY_UPSTREAM`，禁止换工具重试。

### P1-3：合规门禁仍停留在 Draft PR

情报中心 PR #573 已包含机器合规门禁，但尚未接入全部生产工作流，也未合并到 `main`。治理仓库的自然语言宪法同样处于 Draft。

整改：不得只合并文档。应先将统一预检、内容扫描、停止规则、归档门和回归测试接入全部联网 Provider，再进行生产资格验收。

### P1-4：商业数据接口缺少合同许可证明

天眼查代码使用正式开放 API 且删除大量人员字段，技术方法较为合规；但票据和工作流没有要求记录合同是否允许境外 GitHub Actions 调用、缓存、Artifact 保存和派生输出。

中经数据未在当前主分支检索到正式 Provider。因此当前不能认定中经数据已经合规接入。任何会员网页抓取、批量导出、完整数据库复制或者用外国 IP 扩大次数的设计都必须阻断。

整改：每个商业 Provider 建立合同许可矩阵；未上传合同摘要和许可哈希时状态为 `BLOCKED`。

### P2-1：公开 Issue 会暴露任务目标和 URL

任务票据通过公开仓库 Issue 提交。即使没有密钥，任务目的、搜索词、目标 URL、时间范围和数据需求也可能暴露业务策略。

整改：公开 Issue 只能放非敏感任务标识和最小参数；涉及商业研究、未公开计划或敏感目标时，应使用私有仓库或私有控制面。

## 五、能力分类

### 条件合规

以下能力在遵守上游条款、额度和数据分类的前提下，通常可以继续使用：

- 政府或国际组织官方 API；
- 官方 CSV、JSON、XML、SDMX 和公开下载；
- 固定官方主机、只读、单次有界调用的统计、天气、能源、贸易和科研数据接口；
- 不含个人信息和重要数据的公开模型、数据集及空间元数据查询。

### 必须人工复核

- Jina、Exa、Tavily、Firecrawl、Browserless、Cloudflare Browser Rendering；
- Internet Archive 和网页全文提取；
- 天眼查等商业官方 API；
- 航空、船舶、精确地理、卫星和供应链数据的大规模汇聚；
- 任何要保存到 GitHub、Hugging Face、Cloudflare 或提交给 GPT 的原始数据。

### 必须阻断

- 中经、天眼查、企查查等商业数据库网页抓取；
- 会员页面、登录页面和批量导出页面抓取；
- 代理池、住宅代理、IP 轮换和多区域切换规避限制；
- 验证码、WAF、签名、Cookie、设备验证和下载限制绕过；
- 身份认证数据、秘密、重要数据和未经许可的个人信息；
- 完整商业数据库复制、镜像、转售或替代产品；
- 未经许可将原始数据上传境外平台。

## 六、生产整改顺序

1. 将治理层宪法定为唯一权威版本；
2. 把统一合规票据和预检门接入每个联网工作流；
3. 建立数据源与合同许可登记表；
4. 在 Artifact、Hugging Face 和 GPT 之前增加内容级扫描；
5. 公开 GitHub Artifact 改为只保存哈希、摘要和审计回执；
6. 对代理浏览器结果检测 WAF、CAPTCHA、登录和地域拒绝；
7. 建立保存期限、自动删除和事件隔离；
8. 对允许、复核、阻断和停止场景执行全量回归测试；
9. 通过后再将合规 PR 标记为可审查并合并；
10. 未完成前保持生产合规资格 `BLOCKED`。

## 七、最终判断

当前情报中心不是违法采集系统，其整体技术设计已经明显倾向于固定官方接口、有界只读和禁止绕过。

但它仍不能被认定为完全合规，因为“技术安全控制”尚未完整转化为“来源授权、数据分类、境外处理和发布控制”。

只有补齐统一授权票据、实际内容扫描、境外归档限制、公开 Artifact 最小化和全工作流门禁后，系统才具备可证明的生产合规基础。

# 情报中心合规审计报告

审计日期：`2026-08-03`

审计对象：`a15280020511/evidence-data-center`

审计基线：`main@bbae64bda52eb9160b6fc3e62acc9e3010189ff4`

## 一、目标架构澄清

用户的正式要求不是保存原始采集数据，而是：

```text
原始数据仅在单次任务内临时处理
→ 清洗、聚合、分析
→ 只保存分析后的结构化成果
→ 原始数据立即删除
```

Hugging Face可以作为私有分析成果数据库，但不应作为原始数据归档库。GitHub不得保存原始数据，GitHub Artifact也不得保存原始响应。

## 二、当前结论

```text
总体状态：部分合规
技术访问边界：较好
来源授权门：不完整
原始数据临时化：尚未实现
GitHub零原始数据：尚未实现
Hugging Face仅保存分析成果：尚未实现
正式生产合规资格：BLOCKED
```

## 三、风险的本质

风险不在外国IP本身，而在：

```text
有没有权取得
有没有绕过取得
取得的是什么
取得多少
如何分析
最终保存什么
原始内容是否真正销毁
全过程能否证明
```

只保存分析成果，比长期保存原始数据明显降低风险；但分析成果仍必须避免包含个人信息、重要数据、商业数据库的可还原复制品或能替代原平台的数据集合。

## 四、当前做得正确的部分

1. 多数Provider采用固定官方主机、只读操作、单次有界请求、无自动重试和无任意函数设计；
2. Browserless禁止代理、Cookie、自定义请求头、验证码绕过和任意代码；
3. 国家数据网站出现`403/WAF`后，相关直连能力已删除，没有换IP继续绕过；
4. 天眼查使用正式开放API，并删除多类直接个人字段；
5. 多数密钥只在后端注入，不进入任务票据。

## 五、与目标架构不一致的生产级问题

### P0-1：Cloudflare会把完整采集结果上传Hugging Face

当前Cloudflare归档程序会把完整结果文件复制到私有Hugging Face Dataset，并采用追加式版本化保存。

这与“只保存分析后成果”的正式要求相反。

整改：删除完整原始结果归档。Cloudflare结果只能进入临时工作目录；分析完成后仅上传结构化分析结果、来源、哈希和审计回执，随后删除原始文件。

### P0-2：GitHub Artifact仍保存完整结果目录

Cloudflare、网页读取和公司数据工作流会把结果目录上传到GitHub Actions Artifact并保留30天。

这不符合“GitHub不保存原始数据”的要求。

整改：Artifact只能包含任务状态、来源、哈希、分析成果地址和原始文件删除证明。不得包含原始网页、JSON、PDF、截图、DOM或完整商业API响应。

### P0-3：缺少原始数据自动销毁证明

当前工作流没有统一证明：

- 原始文件只存在于临时目录；
- 分析完成后已删除；
- 失败、取消和超时路径也会删除；
- GitHub缓存、Artifact和日志没有残留；
- Hugging Face只收到分析成果。

整改：所有执行路径必须运行清理步骤，并生成不可含原始内容的删除回执。

### P0-4：当前Cloudflare链路只是采集和归档，没有独立分析阶段

现有Cloudflare工作流完成一次采集后直接归档，未看到统一的清洗、聚合、分析和结果过滤阶段。

整改：生产链路改为：

```text
采集临时文件
→ 内容分类
→ 清洗脱敏
→ 分析/聚合
→ 分析结果合规检查
→ 上传私有Hugging Face
→ 删除原始文件
→ GitHub只留回执
```

### P1-1：票据缺少授权和成果保存字段

当前票据多依赖`public`、`contains_personal_data=false`等自我声明，缺少授权依据、自动化许可、外国访问许可、分析目的和成果保存规则。

整改：联网前必须明确来源、授权、范围、分析目标，以及允许保存的最终字段。

### P1-2：代理网页工具仍需识别目标站拒绝

Jina、Exa、Tavily、Firecrawl、Browserless和Cloudflare Browser可能由第三方返回HTTP 200，但正文实际是验证码、WAF或拒绝页面。

整改：分析前必须检测拒绝页；命中后停止，不得更换工具继续。

### P1-3：商业数据仍需要合同边界

中经、天眼查等商业数据即使最终只保存分析结果，也需要确认合同是否允许自动化调用、境外处理和派生结果保存。

会员网页抓取、突破次数、完整数据库复制或可还原的派生数据库必须阻断。

## 六、允许保存到Hugging Face的内容

- 聚合统计；
- 趋势和变化率；
- 分类、评分、标签和模型输出；
- 公开事实的结构化摘要；
- 去标识化且不能还原个人的关系结果；
- 来源机关、来源URL、发布时间、采集时间；
- 原始数据哈希、分析代码版本、方法和审计回执；
- 复核结论所需的最小引用片段。

不得保存：

- 完整原始网页、JSON、PDF、截图、DOM；
- 完整商业API响应；
- 个人身份和联系方式；
- Cookie、Token、验证码；
- 秘密、重要数据；
- 完整中经或其他商业数据库；
- 能够从分析结果重建原始数据库的数据集合。

## 七、生产整改顺序

1. 停止Cloudflare完整结果归档；
2. 删除GitHub完整结果Artifact；
3. 建立临时原始数据目录和全路径自动销毁；
4. 增加清洗、脱敏、聚合和分析阶段；
5. 对最终分析成果执行内容检查；
6. Hugging Face只保存分析成果；
7. GitHub只保存哈希、状态和删除回执；
8. 接入统一授权票据和拒绝信号检测；
9. 测试成功、失败、取消、超时均无原始数据残留；
10. 全部门通过后再解除`BLOCKED`。

## 八、最终判断

你的目标架构是合理的，而且比保存原始数据更安全、更简单：

```text
原始数据：临时使用后删除
分析成果：私有Hugging Face保存
GitHub：只保存代码和审计证明
```

当前生产代码尚未完全实现这个目标。主要问题不是Hugging Face能不能私有保存，而是当前代码仍在保存完整Cloudflare结果和GitHub Artifact。完成上述整改后，整体风险会显著下降。
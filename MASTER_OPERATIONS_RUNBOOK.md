# 总维护与运行手册

## 变更顺序

1. 在对应业务仓库修改并通过本地 CI。
2. 更新能力机器目录，不直接手工维护重复能力表。
3. 更新治理合同冻结副本、版本矩阵和 SHA。
4. GPTs 只在兼容矩阵通过后切换能力目录或 Action。
5. 破坏性变更必须先影子运行，再停用旧入口。

## 故障原则

- 单中心失败不扩散到其他中心。
- 禁止无限重试和无限 fallback。
- 结果未形成完整证据链时不得宣布完成。
- Secret 仅在目标仓库重新配置，不通过代码、Artifact 或迁移包复制。

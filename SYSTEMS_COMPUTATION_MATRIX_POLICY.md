# 系统论计算矩阵治理政策

## 定位

计算矩阵是计算中心内部的确定性控制面，不是第四个中心，也不是新的跨仓库编排服务。

三个业务中心仍然平行、并列、隔离；GPTs 是唯一的跨中心控制与证据中继。治理仓库只保存合同、版本和边界，不在运行时读取或执行计算矩阵。

## 矩阵维度

```text
问题类型
× 系统层级
× 反馈结构
× 证据成熟度
× 决策风险
× 必须通过的质量门
× 固定 operation / mode
× 独立依赖矩阵
```

系统层级至少区分：观察、机制、状态估计、状态演化、决策、验证。

必须显式处理的系统属性包括：反馈、延迟、非线性、适应、路径依赖、容量约束、网络拓扑和真实结果反馈。

## 统一生命周期

```text
数学表述
→ 参数估计或校准
→ 仿真或优化
→ 压测与验证
→ 真实结果反馈
```

每项任务只启用必要阶段。禁止为了形式完整，强制所有任务经过同一条重型全量流水线。

## 失败关闭规则

- 观察结果不得自动提升为机制或因果结论；
- 静态优化不得自动解释为长期动态政策效果；
- 决策类路线必须包含假设治理和压力测试；
- 高风险用途必须要求独立验证和人工审批；
- 未识别、未校准、违反硬约束或缺少冻结验证的模型不得正式发布；
- 真实结果返回后必须进入误差、漂移和模型升降级记录。

## 新工具准入

不得仅因工具知名或功能强而接入。新工具必须同时满足：

```text
现有能力存在可复现缺口
＋冻结基准证明准确度或资源效率显著改善
＋不破坏网络隔离
＋拥有独立依赖矩阵
＋固定输入 Schema 和资源上限
＋具备故障注入、回滚和 Artifact 证据
```

Dagster、MLflow、Ray、Dask、JAX、Stan、PyMC、Ceres、Julia 生态、SMT、Pyomo 等均属于按缺口评估的候选，不是默认基础设施。
